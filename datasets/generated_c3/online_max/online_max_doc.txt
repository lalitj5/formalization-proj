Require that the input array is non-empty and x is a non-zero index into the array. Return an index p that is at least x, where if p is not the last index then all elements before p are strictly less than the element at p, and if no element after position x exceeds the maximum of the first x elements then p is the last index; also ensure the ghost variable represents some value that all first x elements are less than or equal to and that is achieved by at least one of them.