Copies `len` elements from `src` array starting at `sStart` to `dest` array starting at `dStart` and returns the modified `dest` array. Ensures r and dest have the same length and that the copied range in r matches the corresponding range in src.