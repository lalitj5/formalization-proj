This method returns the longest common prefix between two non-empty sequences of characters. Ensure first that the prefix is no longer than the shorter of the two inputs, then ensure that it matches the first few chars of both inputs. Then ensure that it is the longest prefix, which means that either prefix is equal to both of the inputs, or the next character in inputs does not equal.